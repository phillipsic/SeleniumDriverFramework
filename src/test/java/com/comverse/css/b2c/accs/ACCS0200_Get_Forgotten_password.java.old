package com.comverse.css.b2c.accs;

import org.junit.After;
import org.junit.Before;

import com.comverse.css.common.CSSSeleneseTestCase; import com.comverse.css.common.AlreadyRunException;
import com.comverse.css.common.Prep;
import com.comverse.css.common.User;
import com.comverse.css.common.Wait;

public class ACCS0200_Get_Forgotten_password extends CSSSeleneseTestCase {

    private StringBuffer broken_error = new StringBuffer();
    private String bugId = "NoBug";
    private String app = "";
    private String version = "";
    private String deployMode = "CV";
    private String browser = ""; private String result = "fail";

    @Before
    @Override
    public void setUp() {

        String htmlFileName = this.getClass().getSimpleName().toString();

        super.setUp(htmlFileName);

    }

    public void testACCS0200_Get_Forgotten_password() throws Exception {
        try {
            Prep b2cApplication = new Prep(selenium);
            Wait waiting = new Wait(selenium);
            User myUser = new User(selenium);
            String Username = "l" + b2cApplication.generateTimeStamp();
            String UserPassword = myUser.setDefaultPassword();

            app = b2cApplication.launchB2C();
            version = b2cApplication.getBuildNumber(app, this.getClass().getSimpleName().toString());
            browser = b2cApplication.getBrowserAgent();

            myUser.loginSeniorAcctManager();

            selenium.click("mnu_SEARCH_CUSTOMER");
            waiting.forPageToLoad();
            selenium.type("p-L4:1010-L3:1450", "*");
            selenium.click("//form[@id='form_CUSTOMER_RESULTS_2']/fieldset[2]/input[11]");
            waiting.forPageToLoad();
            selenium.click("lnk_CUST_CONTEXT_0");
            waiting.forPageToLoad();
            selenium.click("mnu_MEMBERS");
            waiting.forPageToLoad();
            selenium.click("smnu_REGISTER_MEMBER");
            waiting.forPageToLoad();
            selenium.type("login", Username);
            selenium.type("password", "password$1");
            selenium.type("confirmPassword", "password$1");
            selenium.click("//input[@name='role' and @value='L3:102']");
            selenium.type("lastname", "lastname" + Username);
            selenium.type("firstname", "firstname" + Username);
            selenium.select("contactTitle", "label=Mr.");
            selenium.click("//input[@value='OK']");
            waiting.forPageToLoad();
            assertTrue(selenium.isTextPresent("Register New Employee"));
            assertTrue(selenium.isTextPresent("Confirm This User Creation"));
            selenium.click("//input[@value='Confirm']");
            waiting.forPageToLoad();
            assertTrue(selenium.isTextPresent("Request Submission"));
            assertTrue(selenium.isTextPresent("The \"Register New Employee\" has been submitted successfully."));
            selenium.click("//input[@value='OK']");
            waiting.forPageToLoad();
            myUser.logout();
            

            selenium.type("login", Username);
            selenium.type("password", "password$1");
            selenium.click("LoginButton");
            waiting.forPageToLoad();
            assertTrue(selenium.isTextPresent("You must change your password"));
            selenium.type("password_old", "password$1");
            selenium.type("password_new", UserPassword);
            selenium.type("password_verify", UserPassword);
            selenium.type("secretAnswer", UserPassword);
            selenium.click("//input[@value='Change']");
            waiting.forTextPresent("Welcome to Your Personalized Workspace");

            myUser.logout();

            selenium.type("login", Username);
            selenium.click("lnk_FORGOTTEN_PASSWORD");
            waiting.forTextPresent("Please enter your secret answer and a new temporary password.");
            selenium.type("password_new", "0123456$b");
            selenium.type("password_verify", "0123456$b");
            selenium.type("secretAnswer", UserPassword);
            assertTrue(selenium.isTextPresent("What 's your mother 's last name?"));
            selenium.click("//input[@value='OK']");
            waiting.forTextPresent("New password set properly.");
            selenium.type("login", Username);
            selenium.type("password", "0123456$b");
            selenium.click("LoginButton");
            waiting.forTextPresent("Welcome to Your Personalized Workspace");
        result = "pass"; } catch (AlreadyRunException e) {         } catch (Exception e) {
            broken_error.append(e.getMessage());
            throw e;
        }
    }

    @After
    @Override
    public void tearDown() {

        //String result = super.testResult(broken_error);
        String testId = this.getClass().getSimpleName().toString();
        super.logResults(bugId, app, version, testId, result, deployMode, browser);

        super.tearDown();
    }
}
