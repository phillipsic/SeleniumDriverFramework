package com.comverse.css.b2b.aca;

import org.junit.After;
import org.junit.Before;

import com.comverse.css.b2b.Navigation;
import com.comverse.css.common.AlreadyRunException;
import com.comverse.css.common.CSSSeleneseTestCase;
import com.comverse.css.common.Prep;
import com.comverse.css.common.User;
import com.comverse.css.common.Wait;

public class ACA0050_View_subscriber_combined_history extends CSSSeleneseTestCase {

    private StringBuffer broken_error = new StringBuffer();
    private String bugId = "NoBug";
    private String app = "";
    private String version = "";
    private String deployMode = "CV";
    private String browser = "";
    private String result = "fail";

    @Before
    @Override
    public void setUp() {
        String htmlFileName = this.getClass().getSimpleName().toString();
        super.setUp(htmlFileName);
    }

    public void testACA0050_View_subscriber_combined_history() throws Exception {
        try {
            Prep b2bApplication = new Prep(selenium);
            User myUser = new User(selenium);
            Wait waiting = new Wait(selenium);
            Navigation doNav = new Navigation(selenium);
            //PropertyHelper propsHelper = new PropertyHelper();
            //String Invoice = propsHelper.getAccountProperties("Invoice.RC");

            app = b2bApplication.launchB2B();
            version = b2bApplication.getBuildNumber(app, this.getClass().getSimpleName().toString());
            browser = b2bApplication.getBrowserAgent();

            myUser.loginB2BAdmin();

            doNav.gotoSubscriberPage();
            selenium.click("viewCombinedHistory");
            waiting.forPageToLoad();

            assertEquals("View combined history", selenium.getTitle());
            selenium.type("fromDate", "01/01/2000");
            selenium.removeSelection("R_SYSTEM_Combined_History_P_TRANSACTION_TYPES_PROMPT", "label=Adjustment");
            selenium.removeSelection("R_SYSTEM_Combined_History_P_TRANSACTION_TYPES_PROMPT", "label=NRC");
            selenium.removeSelection("R_SYSTEM_Combined_History_P_TRANSACTION_TYPES_PROMPT", "label=RC");
            selenium.removeSelection("R_SYSTEM_Combined_History_P_TRANSACTION_TYPES_PROMPT", "label=Recharge");
            selenium.removeSelection("R_SYSTEM_Combined_History_P_TRANSACTION_TYPES_PROMPT", "label=Usage");
            selenium.click("//input[@value='Search' and @type='submit']");
            waiting.forPageToLoad();

            assertTrue(selenium.isTextPresent("Combined history found"));
            selenium.click("transactionDetails");
            waiting.forPageToLoad();

            assertEquals("View MTR details", selenium.getTitle());
            assertTrue(selenium.isTextPresent("MTR details"));
            assertTrue(selenium.isTextPresent("Impacted Balances"));
            selenium.click("//input[@value='Back']");
            waiting.forPageToLoad();

            assertEquals("View combined history", selenium.getTitle());

            result = "pass";
        } catch (AlreadyRunException e) {
        } catch (Exception e) {
            broken_error.append(e.getMessage());
            throw e;
        }
    }

    @After
    @Override
    public void tearDown() {
        //String result = super.testResult(broken_error);
        String testId = this.getClass().getSimpleName().toString();
        super.logResults(bugId, app, version, testId, result, deployMode, browser);
        super.tearDown();
    }

}