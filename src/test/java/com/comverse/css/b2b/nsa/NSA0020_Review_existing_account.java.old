package com.comverse.css.b2b.nsa;

import org.junit.After;
import org.junit.Before;

import com.comverse.css.common.CSSSeleneseTestCase; import com.comverse.css.common.AlreadyRunException;
import com.comverse.css.common.Prep;
import com.comverse.css.common.User;
import com.comverse.css.common.Wait;

public class NSA0020_Review_existing_account extends CSSSeleneseTestCase {

    private StringBuffer broken_error = new StringBuffer();
    private String bugId = "NoBug";
    private String app = "";
    private String version = "";
    private String deployMode = "CV";
    private String browser = "";
    private String result = "fail";

    @Before
    @Override
    public void setUp() {
        String htmlFileName = this.getClass().getSimpleName().toString();
        super.setUp(htmlFileName);
    }

    public void testNSA0020_Review_existing_account() throws Exception {
        try {
            Prep b2bApplication = new Prep(selenium);
            User myUser = new User(selenium);
            Wait waiting = new Wait(selenium);

            app = b2bApplication.launchB2B();
            version = b2bApplication.getBuildNumber(app, this.getClass().getSimpleName().toString());
            browser = b2bApplication.getBrowserAgent();

            myUser.loginB2BAdmin();

            selenium.click("//a[@id='mnu_ACCOUNT']");
            waiting.forPageToLoad();

            selenium.click("//a[@id='firstYouCanconvergentExistingAccountMenu']");
            waiting.forPageToLoad();

            assertTrue(selenium.getTitle().equals("Customer Acquisition - Customer Data Review"));
            //assertTrue(selenium.isTextPresent("sdf"));
            //assertTrue(selenium.isTextPresent("dsfsdfsdfsd"));
            //assertTrue(selenium.isTextPresent("dsf"));
            assertTrue(selenium.isTextPresent("Paperless"));
            assertTrue(selenium.isTextPresent("Please Review the following Customer Data is correct"));
            assertTrue(selenium.isTextPresent("France"));
            assertTrue(selenium.isTextPresent("Customer identity and contact"));
            assertTrue(selenium.isTextPresent("Customer Address details"));
            assertTrue(selenium.isTextPresent("Billing Information"));
            assertTrue(selenium.isTextPresent("Payment method detail"));

            result = "pass";
        } catch (AlreadyRunException e) {         } catch (Exception e) {
            broken_error.append(e.getMessage());
            throw e;
        }
    }

    @After
    @Override
    public void tearDown() {
        //String result = super.testResult(broken_error);
        String testId = this.getClass().getSimpleName().toString();
        super.logResults(bugId, app, version, testId, result, deployMode, browser);
        super.tearDown();
    }

}
