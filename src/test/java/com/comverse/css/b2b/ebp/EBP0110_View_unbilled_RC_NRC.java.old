package com.comverse.css.b2b.ebp;

import org.junit.After;
import org.junit.Before;

import com.comverse.css.common.AlreadyRunException;
import com.comverse.css.common.CSSSeleneseTestCase;
import com.comverse.css.common.Prep;
import com.comverse.css.common.User;
import com.comverse.css.common.Wait;

public class EBP0110_View_unbilled_RC_NRC extends CSSSeleneseTestCase {

    private StringBuffer broken_error = new StringBuffer();
    private String bugId = "NoBug";
    private String app = "";
    private String version = "";
    private String deployMode = "CV";
    private String browser = "";
    private String result = "fail";

    @Before
    @Override
    public void setUp() {
        String htmlFileName = this.getClass().getSimpleName().toString();
        super.setUp(htmlFileName);
    }

    public void testEBP0110_View_unbilled_RC_NRC() throws Exception {
        try {
            Prep b2bApplication = new Prep(selenium);
            User myUser = new User(selenium);
            Wait waiting = new Wait(selenium);
            //PropertyHelper propsHelper = new PropertyHelper();
            //String Invoice = propsHelper.getAccountProperties("Unbilled.RC_NRC");

            app = b2bApplication.launchB2B();
            version = b2bApplication.getBuildNumber(app, this.getClass().getSimpleName().toString());
            browser = b2bApplication.getBrowserAgent();

            myUser.loginB2BAdmin();

            selenium.click("//a[@id='mnu_ACCOUNT']");
            waiting.forPageToLoad();

            selenium.click("mnu_BILLS_PAYMENTS");
            waiting.forPageToLoad();

            assertEquals("View invoices", selenium.getTitle());
            selenium.click("smnu_UNBILLED_TRANSACTIONS");
            waiting.forPageToLoad();

            assertEquals("View unbilled adjustments", selenium.getTitle());
            selenium.click("youcan_VIEW_RC_NRC");
            waiting.forPageToLoad();

            assertEquals("View Unbilled RC/NRC", selenium.getTitle());
            assertTrue(selenium.isTextPresent("Unbilled RC/NRC terms"));
            assertTrue(selenium.isTextPresent("Non recurring charges for this account:"));
            assertTrue(selenium.isTextPresent("Recurring charges for this account:"));
            selenium.click("//html/body/table/tbody/tr[2]/td[2]/div/div[2]/div/fieldset/div[4]/div/table/tbody[2]/tr/td/div/a");
            waiting.forPageToLoad();

            assertEquals("View unbilled RC term details", selenium.getTitle());
            assertTrue(selenium.isTextPresent("Unbilled RC term details"));
            assertTrue(selenium.isTextPresent("Impacted Balances"));
            selenium.click("//input[@value='Back']");
            waiting.forPageToLoad();

            assertEquals("View Unbilled RC/NRC", selenium.getTitle());
            selenium.click("//html/body/table/tbody/tr[2]/td[2]/div/div[2]/div/fieldset/div[2]/div/table/tbody[2]/tr/td/div/a");
            waiting.forPageToLoad();

            assertEquals("View unbilled NRC term details", selenium.getTitle());
            assertTrue(selenium.isTextPresent("Unbilled NRC term details"));
            assertTrue(selenium.isTextPresent("Impacted Balances"));
            selenium.click("//input[@value='Back']");
            waiting.forPageToLoad();

            assertEquals("View Unbilled RC/NRC", selenium.getTitle());
            selenium.click("//input[@value='Back']");
            waiting.forPageToLoad();

            assertEquals("View unbilled adjustments", selenium.getTitle());

            result = "pass";
        } catch (AlreadyRunException e) {
        } catch (Exception e) {
            broken_error.append(e.getMessage());
            throw e;
        }
    }

    @After
    @Override
    public void tearDown() {
        //String result = super.testResult(broken_error);
        String testId = this.getClass().getSimpleName().toString();
        super.logResults(bugId, app, version, testId, result, deployMode, browser);
        super.tearDown();
    }

}