/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
/**
 *
 * @author Vikram Sharma
 */
package com.comverse.css.bct;

import com.comverse.css.common.CSSTest;
import com.comverse.css.csr.HealthPage;
import org.junit.After;
import org.junit.Before;
import org.junit.Test;

/**
 *
 * @author Vikram Sharma
 */
public class BCT008_Environment_Health_Check extends CSSTest {

    @Override
    @Before
    public void setUp() throws Exception {

        super.launchHealthCheckApplication();
        application = null;

    }

    @Test
    public void testBCT008_Environment_Health_Check() throws Exception {

        HealthPage HealthPage = new HealthPage(driver, sapiUrl);

        boolean crmresult = HealthPage.crmhealthcheck(sapiUrl + "/crmHealthCheck.jsp", "ERROR:");
        boolean rcsresult = HealthPage.rcsHealthCheck(sapiUrl + "/rcsHealthCheck.jsp", "ERROR:");
        boolean rhtresult = HealthPage.rhtHealthCheck(sapiUrl + "/rhtHealthCheck.jsp", "ERROR:");
        // boolean obcresult = HealthPage.obcHealthcheck(sapiUrl + "/obcHealthCheck.jsp", "ERROR:");
        boolean obcresult = false;
        boolean dbresult = HealthPage.dbHealthCheck(sapiUrl + "/db_capacity.jsp", "BAD");

        if (crmresult == true || rcsresult == true || rhtresult == true || obcresult == true || dbresult == true) {

            throw new IllegalStateException("Errors have been reported in the environment");
        }

        result = "pass";

    }

    @Override
    @After
    public void tearDown() throws Exception {

        super.tearDown();
    }
}
